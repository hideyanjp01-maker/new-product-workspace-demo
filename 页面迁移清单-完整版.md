# 页面迁移清单（完整版）

## 优先级1：Stages 四页 ✅ 已完成（4/4）

1. ✅ **洞察期** - `src/pages/stages/InsightStagePage.tsx` - `/stages/insight`
2. ✅ **企划期** - `src/pages/stages/PlanningStagePage.tsx` - `/stages/planning`
3. ✅ **冷启动期** - `src/pages/stages/ColdStartStagePage.tsx` - `/stages/cold-start`
4. ✅ **放量期** - `src/pages/stages/ScaleUpStagePage.tsx` - `/stages/scale-up`

## 优先级2：角色总览（待确认）

5. **角色总览** - 需要确认是否需要单独的角色总览页面，或使用现有的 HomePage

## 优先级3：角色详情页（18个）

根据 workflowStore.ts 和 mockData.ts，共有18个角色页面需要迁移：

6. **新品灵感洞察器** - `/roles/new-product-insight` - roleId: `new-product-insight`
7. **AI洞察** - `/roles/ai-insight` - roleId: `ai-insight`
8. **市场分析** - `/roles/market-analysis` - roleId: `market-analysis`
9. **品牌负责人** - `/roles/brand-manager` - roleId: `brand-manager`
10. **商品管理** - `/roles/product-management` - roleId: `product-management`
11. **电商负责人** - `/roles/ecommerce-director` - roleId: `ecommerce-director`
12. **天猫运营** - `/roles/tmall-operation` - roleId: `tmall-operation`
13. **天猫流量运营** - `/roles/tmall-traffic` - roleId: `tmall-traffic`
14. **京东运营** - `/roles/jd-operation` - roleId: `jd-operation`
15. **京东流量运营** - `/roles/jd-traffic` - roleId: `jd-traffic`
16. **抖音店铺** - `/roles/douyin-shop` - roleId: `douyin-shop`
17. **抖音投放** - `/roles/douyin-advertising` - roleId: `douyin-advertising`
18. **抖音内容制作** - `/roles/douyin-content` - roleId: `douyin-content`
19. **抖音商务** - `/roles/douyin-business` - roleId: `douyin-business`
20. **潜力达人洞察** - `/roles/talent-insight` - roleId: `talent-insight`
21. **竞争品VS** - `/roles/competitor-analysis` - roleId: `competitor-analysis`
22. **素材生成器** - `/roles/material-generator` - roleId: `material-generator`
23. **任务** - `/roles/task` - roleId: `task`

## 当前状态

- ✅ Stages 四页：已完成（4/4）
- ⏳ 角色总览：待确认
- ⏳ 角色详情页：0/18（当前使用 RoleDetailPage 动态路由，显示静态图片）

## 路由说明

当前路由 `/roles/:roleId` 使用动态路由，由 `RoleDetailPage.tsx` 处理，显示静态图片。

迁移后需要：
- 为每个角色创建独立的组件化页面
- 或创建统一的 RoleDetailPage 组件，根据 roleId 动态加载对应内容
- 集成全局 store（dateA/dateB、viewMode、selectedProductId）
- 实现真实的组件化 UI，不再使用静态图片

## 下一步

等待用户确认：
1. "角色总览"页面是否需要单独创建？
2. 18个角色页面是创建独立组件，还是使用统一的动态组件？




